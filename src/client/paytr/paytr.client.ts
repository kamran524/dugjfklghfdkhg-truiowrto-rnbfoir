import envConfig from "../../config/env.config";
import logger from "../../log/winston";

class PayTRCLient {
  constructor() {
  }
  
  public async getIFrameToken() {
    logger.debug(`${PayTRCLient.name}.getIFrameToken -- start`);
    try {
      
    } catch (error) {
      
    }
    logger.debug(`${PayTRCLient.name}.getIFrameToken -- success`);
  }
}

export default PayTRCLient;

